# Open problems

## Release activation

The independent annual feed and its client are implemented and tested locally.
The workflow still needs to land on the fork's default branch, publish its first
`annual-data` snapshot, and have the client change deployed. Its public URL and
scheduled execution cannot be verified before that activation. See
[MAINTAINING.md](MAINTAINING.md) for the exact activation steps.
